package FinalExam;

/*
public class longestWordFinder {
    public static String findLongestWord(String input) {//Defines a public static method named findLongestWord. It takes a String parameter called input and returns a String.

        String[] words = input.split("\\s+");//Splits the input string into an array of words using whitespace as the separator and assigns it to the words array.
        String longestWord = "";// Initializes an empty String named longestWord. This variable will store the longest word found.

        for (String word : words) {// Initiates a loop that iterates through each word in the words array.
            if (word.length() > longestWord.length()) {
                longestWord = word;
                //Inside the loop:
                //Checks if the length of the current word is greater than the length of the current longestWord.
                //If true, updates longestWord with the current word.
            }
        }  

        return longestWord;//: Returns the longest word found after iterating through all words in the input string.
    }

    //In summary, the longestWordFinder class provides a method, findLongestWord, that takes a sentence as input, splits it into words, and finds the longest word in the sentence.
    // The longest word is determined by comparing the lengths of each word in the input. The method then returns the longest word found.
}

 */